{
  "// ": [
    "Kiểm kiểu cho thư mục api/ — tsconfig.json chính chỉ include 'src' nên toàn bộ",
    "backend KHÔNG được kiểm, `npm run build` xanh mà api/ vẫn có thể hỏng.",
    "Đã dính thật: sửa import thiếu đuôi .js, tsc báo OK, lên Vercel chết ngay lúc gọi.",
    "Chạy: npm run check:api"
  ],
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "skipLibCheck": true,
    "noEmit": true,
    "isolatedModules": true,
    "resolveJsonModule": true
  },
  "include": ["api"]
}
